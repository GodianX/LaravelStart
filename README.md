# LaravelStart

### System requirements

- Docker 

### Installation

- `cd docker`
- `docker-compose up -d --build`
- `docker exec -it laravel_php bash`
- `cd ../laravelStart/`
- `composer install`

### Web

- `http://127.0.0.1:1080/`


### Postgres

- host: `laravel_postgres`
- user: `laravel`
- password: `laravel`
- database: `postgres`
- port: `5432`
